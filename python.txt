# 가상환경 활성화 상태에서 실행
pip install --no-cache-dir https://developer.download.nvidia.com/compute/redist/jp/v60/pytorch/torch-2.3.0%2Bebedce2.nv24.03-cp310-cp310-linux_aarch64.whl

pip uninstall numpy -y
pip install "numpy<2"

# 1. 가상환경 탈출
deactivate

# 2. 호스트 시스템에 설치
sudo pip3 install -U jetson-stats

# 3. 서비스 재시작 (필요시)
sudo systemctl restart jetson_stats.service

# 4. 다시 가상환경 접속 후 설치
cd ~/new_orin_project
source venv/bin/activate
pip install jetson-stats

# 1. 넘파이 복구
pip uninstall numpy -y
pip install "numpy<2"

# 2. 토치 재시도
pip install https://developer.download.nvidia.com/compute/redist/jp/v60/pytorch/torch-2.3.0%2Bebedce2.nv24.03-cp310-cp310-linux_aarch64.whl

# 3. 토치비전 (토치 설치 성공 후 실행)
pip install torchvision==0.18.0

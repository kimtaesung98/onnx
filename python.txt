import cv2
import numpy as np
import onnxruntime as ort
import time

# 1. Load ONNX with TensorRT Provider
# This tells the Jetson to build a hardware-specific engine
providers = [
    ('TensorRTExecutionProvider', {
        'device_id': 0,
        'trt_fp16_enable': True,  # This is the Quantization trigger!
        'trt_engine_cache_enable': True,
        'trt_engine_cache_path': './'
    }),
    'CUDAExecutionProvider'
]

print("Initializing TensorRT Engine... (This may take a minute on first run)")
session = ort.InferenceSession("xfeat_fp16.onnx", providers=providers)

# 2. Camera Setup
cap = cv2.VideoCapture(0)
cap.set(cv2.CAP_PROP_FRAME_WIDTH, 640)
cap.set(cv2.CAP_PROP_FRAME_HEIGHT, 480)

while True:
    ret, frame = cap.read()
    if not ret: break
    
    start = time.time()
    
    # Pre-processing: Grayscale and Normalize
    gray = cv2.cvtColor(frame, cv2.COLOR_BGR2GRAY)
    inp = gray.astype(np.float32) / 255.0
    inp = inp[None, None, :, :] # Add batch and channel dims

    # Inference (The Quantized Speed)
    outputs = session.run(None, {'input': inp})
    
    # ... (Feature extraction logic here) ...

    fps = 1.0 / (time.time() - start)
    cv2.putText(frame, f"QUANTIZED FPS: {fps:.1f}", (20, 50), 1, 2, (0, 255, 0), 2)
    cv2.imshow("Jetson Nano Optimized SLAM", frame)
    
    if cv2.waitKey(1) == ord('q'): break

cap.release()

(venv) prlab@prlab-desktop:~/jetson_camera_project$ /home/prlab/jetson_camera_project/venv/bin/python /home/prlab/jetson_camera_project/xfeat_research/live_slam_quantized.py
2026-04-10 16:26:05.810991786 [W:onnxruntime:Default, device_discovery.cc:164 DiscoverDevicesForPlatform] GPU device discovery failed: device_discovery.cc:89 ReadFileContents Failed to open file: "/sys/class/drm/card1/device/vendor"
Initializing TensorRT Engine... (This may take a minute on first run)
/home/prlab/jetson_camera_project/venv/lib/python3.10/site-packages/onnxruntime/capi/onnxruntime_inference_collection.py:123: UserWarning: Specified provider 'TensorRTExecutionProvider' is not in available provider names.Available providers: 'AzureExecutionProvider, CPUExecutionProvider'
  warnings.warn(
/home/prlab/jetson_camera_project/venv/lib/python3.10/site-packages/onnxruntime/capi/onnxruntime_inference_collection.py:123: UserWarning: Specified provider 'CUDAExecutionProvider' is not in available provider names.Available providers: 'AzureExecutionProvider, CPUExecutionProvider'
  warnings.warn(
*************** EP Error ***************
EP Error Unknown Provider Type: TensorRTExecutionProvider when using [('TensorRTExecutionProvider', {'device_id': 0, 'trt_fp16_enable': True, 'trt_engine_cache_enable': True, 'trt_engine_cache_path': './'}), 'CUDAExecutionProvider']
Falling back to ['CPUExecutionProvider'] and retrying.
****************************************
Traceback (most recent call last):
  File "/home/prlab/jetson_camera_project/xfeat_research/live_slam_quantized.py", line 38, in <module>
    outputs = session.run(None, {'input': inp})
  File "/home/prlab/jetson_camera_project/venv/lib/python3.10/site-packages/onnxruntime/capi/onnxruntime_inference_collection.py", line 287, in run
    return self._sess.run(output_names, input_feed, run_options)
onnxruntime.capi.onnxruntime_pybind11_state.InvalidArgument: [ONNXRuntimeError] : 2 : INVALID_ARGUMENT : Unexpected input data type. Actual: (tensor(float)) , expected: (tensor(float16))
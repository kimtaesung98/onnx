# 1. 시스템의 라이브러리 위치를 확인 (보통 여기에 있습니다)
ls /usr/lib/aarch64-linux-gnu/libcudnn.so.8

# 2. 경로를 환경 변수에 추가
export LD_LIBRARY_PATH=$LD_LIBRARY_PATH:/usr/local/cuda/lib64:/usr/lib/aarch64-linux-gnu

# 3. 가상환경이 켜진 상태에서 바로 실행
python3 xfeat_research/orin_realtime_slam_v3.py

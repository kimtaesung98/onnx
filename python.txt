pip install --upgrade pip
# CUDA 12.6과 호환되는 최신 ONNX Runtime GPU 설치

# 기존에 꼬여있을 수 있는 패키지 삭제
pip uninstall onnxruntime-gpu -y

# NVIDIA 공식 Jetson 전용 서버 주소를 사용합니다.
pip install --no-cache-dir --extra-index-url https://pypi.nvidia.com onnxruntime-gpu

# 가상환경 활성화 스크립트 끝에 경로 추가
echo 'export LD_LIBRARY_PATH=$LD_LIBRARY_PATH:/usr/local/cuda/lib64:/usr/lib/aarch64-linux-gnu' >> venv/bin/activate
source venv/bin/activate

# libcudnn (9 -> 8 연결)
sudo ln -sf /usr/lib/aarch64-linux-gnu/libcudnn.so.9 /usr/lib/aarch64-linux-gnu/libcudnn.so.8
# libnvinfer (TensorRT 10/9 -> 8 연결)
sudo ln -sf /usr/lib/aarch64-linux-gnu/libnvinfer.so.10 /usr/lib/aarch64-linux-gnu/libnvinfer.so.8
sudo ldconfig

import onnxruntime as ort
import os

print(f"--- Orin Nano JetPack 6.2 환경 체크 ---")
providers = ort.get_available_providers()
print(f"사용 가능한 공급자: {providers}")

if 'TensorrtExecutionProvider' in providers:
    print("✅ 성공: TensorRT 가속을 사용할 수 있습니다!")
else:
    print("❌ 실패: 여전히 CPU 모드입니다. 라이브러리 링크를 확인하세요.")


    error 

pip install --extra-index-url https://aiinfra.pkgs.visualstudio.com/PublicPackages/_packaging/onnxruntime-cuda-12/pypi/simple/ onnxruntime-gpu
ERROR: Could not find a version that satisfies the requirement onnxruntime-gpu (from versions: none)
ERROR: No matching distribution found for onnxruntime-gpu
